# ============================================
# TradingEdge - Streamlit Cloud
# Only the packages the Streamlit UI actually imports.
# Backend deps live in requirements-backend.txt
# (used by Vercel & Docker).
# ============================================
streamlit>=1.41,<2
httpx>=0.28,<1
